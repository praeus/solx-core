{
  "inputs": [
    {
      "type": "promptString",
      "id": "solxServerToken",
      "description": "solx-server bearer token (solx-config.json's server_token)",
      "password": true
    }
  ],
  "servers": {
    "solx": {
      "type": "http",
      "url": "http://127.0.0.1:8766/mcp",
      "headers": {
        "Authorization": "Bearer ${input:solxServerToken}"
      }
    }
  }
}
