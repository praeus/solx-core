[package]
name = "solx-manager"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Wires the local solx manager implementations together (the App/Solx assembly), shared by solx-cli and solx-mcp."

[dependencies]
solx-surface = { workspace = true }
solx-config = { workspace = true }
solx-types = { workspace = true }
solx-files = { workspace = true }
solx-docs = { workspace = true }
solx-actions = { workspace = true }
anyhow = { workspace = true }
